{
  "name": "depth-explorer",
  "type": "module",
  "private": true,
  "imports": {
    "#config": "./src/config/index.ts",
    "#lib/*": "./src/lib/*.ts"
  },
  "scripts": {
    "build": "bunx --bun usts build",
    "start": "bun --bun run src/node"
  },
  "dependencies": {
    "playwright": "^1.57.0"
  },
  "devDependencies": {
    "@infinite-craft/dom-types": "^0.0.3",
    "@types/bun": "^1.3.11",
    "@violentmonkey/types": "^0.3.2",
    "typescript": "^6.0.2",
    "usts": "^0.1.0"
  }
}
